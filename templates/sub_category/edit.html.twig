{% extends 'base.html.twig' %}

{% block title %}Edit SubCategory{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modify the sub category</h1>

        {{ include('sub_category/_form.html.twig', {'button_label': 'Modify'}) }}

        <a href="{{ path('app_sub_category_index') }}">back to list</a>

        {{ include('sub_category/_delete_form.html.twig') }}
    </div>
{% endblock %}
