{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1 class="my-4">Categories</h1>

                {% include 'layouts/_flash_message.html.twig' %}

                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Name</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for category in categories %}
                                <tr>
                                    <td>{{ category.id }}</td>
                                    <td>{{ category.name }}</td>
                                    <td>
                                        <a href="{{ path('app_categort_update', {'id': category.id}) }}" class="btn btn-primary">Modify</a>
                                        <a href="{{ path('app_categort_delete', {'id': category.id}) }}" class="btn btn-danger">Delete</a>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="3" class="text-center">No categories found</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        <a href="{{ path('app_categort_new') }}" class="btn btn-success">Add Category</a>
    </div>
{% endblock %}
